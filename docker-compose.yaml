version: '3'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./company_structure:/app
    command: >
      bash -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000 && python manage.py loaddata employees/fixtures/*.json"
